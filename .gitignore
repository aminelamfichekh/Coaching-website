# OS junk
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
$RECYCLE.BIN/

# Editor/IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Node / build
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local backups produced by Part B Phase 2
/_original_backup/

# Plugin source is extracted from tracked /plugins/*.zip — regenerate with
# `unzip plugins/booked.zip -d _plugin-source/ && unzip plugins/revslider.zip -d _plugin-source/`
/_plugin-source/

# Scratch / temp
*.tmp
*.log
/tmp-fetch/
/.asset-queue.tmp
# Earlier bash/Python /tmp path collision produced these artifacts at the
# repo root — regenerate via scripts/fetch-remote.sh if ever reproduced.
/static-siteassets*
